extends layout

block content
  h1= title
  p This is a #{title}. It'll get better 🤞

  h1 Dynamic content (we can change this later)

  if error
    p Error getting dynamic content.
  else
    p The collection has the following counts 

    ul 
      li #[strong Movies:] !{data.movie_count}
      li #[strong Directors:] !{data.director_count}
      li #[strong Genres:] !{data.genre_count}
